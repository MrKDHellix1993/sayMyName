import { MyParserDirective } from './my-parser.directive';

describe('MyParserDirective', () => {
  it('should create an instance', () => {
    const directive = new MyParserDirective();
    expect(directive).toBeTruthy();
  });
});
