import { ValidationFunction } from './validation-function';

describe('ValidationFunction', () => {
  it('should create an instance', () => {
    expect(new ValidationFunction()).toBeTruthy();
  });
});
