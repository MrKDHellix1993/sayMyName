import { AlphaCountDirective } from './alpha-count.directive';

describe('AlphaCountDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphaCountDirective();
    expect(directive).toBeTruthy();
  });
});
